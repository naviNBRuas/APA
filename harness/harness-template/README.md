# Harness Template

This is a template for creating new integration/system test harnesses for the APA project.

## Usage
- Copy this folder and rename it for your scenario (e.g., `networking-demo/`).
- Edit the `docker-compose.yml` and this `README.md` as needed.
- Add any scripts or shared folders required for your test.
