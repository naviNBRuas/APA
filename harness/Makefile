# Common harness commands

up:
	docker compose -f p2p/docker-compose.yml up -d --build

down:
	docker compose -f p2p/docker-compose.yml down

logs:
	docker compose -f p2p/docker-compose.yml logs -f

clean:
	docker compose -f p2p/docker-compose.yml down -v
